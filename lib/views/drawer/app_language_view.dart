import 'package:flutter/material.dart';

class AppLanguageView extends StatelessWidget {
  const AppLanguageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('App Language View');
  }
}