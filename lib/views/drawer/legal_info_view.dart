import 'package:flutter/material.dart';

class LegalInfoView extends StatelessWidget {
  const LegalInfoView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Legal Info Page'),
      ),
    );
  }
}
